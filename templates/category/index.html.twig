{% extends 'base.html.twig' %}

{% block main_container %}
    <h1>List of categories</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Colour (RGB)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {% for category in categories %}
                <tr>
                    <td>{{ category.name }}</td>
                    <td>{{ category.colour }}</td>
                    <td>
                        <a class="btn btn-link" href="{{ path('app_category_edit', {id:category.id}) }}" title="Edit">Edit</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}

